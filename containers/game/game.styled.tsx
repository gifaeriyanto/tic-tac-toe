import styled from '@emotion/styled';

export const Row = styled.div({
  display: 'flex',
});

export const Board = styled.div({
  backgroundColor: '#2d256f',
  minHeight: '100vh',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
